name: Hola Mundo Workflow
hosts: localhost

on:
  push:
    branches:
      - main

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Say Hola Mundo
        run: echo "Conectado"
